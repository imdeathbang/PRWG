[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name="prwg"
version="0.1.0"
requires-python = ">=3.9"

[project.scripts]
prwg = "prwg_imdeathbang.generate:main"